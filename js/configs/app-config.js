/**
 * Конфигурация приложения Randomatched
 * Содержит основные настройки приложения, включая версию кэша для PWA
 * Совместимо с importScripts для Service Worker
 */

// Версия кэша для обновления PWA при изменении файлов
// При изменении этой версии Service Worker автоматически обновит кэш
var CACHE_VERSION = 'v1.0.38';

// Дополнительные настройки приложения
var APP_CONFIG = {
  // Название приложения
  name: 'Randomatched',
  
  // Версия приложения (может отличаться от версии кэша)
  version: '1.0.24',
  
  // Описание приложения
  description: 'PWA для случайного выбора героев в Unmatched',
  
  // Настройки кэширования
  cache: {
    // Имя кэша (формируется автоматически с версией)
    name: 'randomatched-cache-' + CACHE_VERSION,
    
    // Стратегия кэширования
    strategy: 'cache-first',
    
    // Время жизни кэша (в миллисекундах)
    ttl: 7 * 24 * 60 * 60 * 1000, // 7 дней
  },
  
  // Настройки уведомлений
  notifications: {
    enabled: true,
    title: 'Randomatched',
    icon: '/icons/icon-192.png',
    badge: '/icons/icon-192.png',
  },
  
  // Настройки обновления
  update: {
    // Автоматическая проверка обновлений
    autoCheck: true,
    
    // Интервал проверки обновлений (в миллисекундах)
    checkInterval: 60 * 60 * 1000, // 1 час
    
    // Принудительное обновление при изменении версии
    forceUpdate: true,
  },

  // Цветовая тема приложения (Material Design 3)
  // Содержит полную палитру цветов для светлой и темной тем
  // Все цвета соответствуют спецификации Material Design 3
  theme: {
    // Светлая тема - основная цветовая схема для дневного режима
    light: {
      // Основные цвета - используются для ключевых элементов интерфейса
      // (кнопки, ссылки, активные состояния)
      primary: {
        main: '#6750a4',        // Основной цвет (фиолетовый)
        onPrimary: '#ffffff',   // Цвет текста/иконок на основном цвете
        container: '#eaddff',   // Цвет контейнера с основным цветом
        onContainer: '#21005d'  // Цвет текста/иконок на контейнере
      },
      
      // Вторичные цвета - используются для менее важных элементов
      // (вторичные кнопки, индикаторы, акценты)
      secondary: {
        main: '#625b71',        // Основной вторичный цвет (серо-фиолетовый)
        onSecondary: '#ffffff', // Цвет текста/иконок на вторичном цвете
        container: '#e8def8',   // Цвет контейнера с вторичным цветом
        onContainer: '#1d192b'  // Цвет текста/иконок на контейнере
      },
      
      // Третичные цвета - используются для дополнительных акцентов
      // (третичные кнопки, специальные индикаторы)
      tertiary: {
        main: '#7d5260',        // Основной третичный цвет (розово-коричневый)
        onTertiary: '#ffffff',  // Цвет текста/иконок на третичном цвете
        container: '#ffd8e4',   // Цвет контейнера с третичным цветом
        onContainer: '#31111d'  // Цвет текста/иконок на контейнере
      },
      
      // Цвета ошибок - используются для отображения ошибок и предупреждений
      error: {
        main: '#ba1a1a',        // Основной цвет ошибки (красный)
        onError: '#ffffff',     // Цвет текста/иконок на цвете ошибки
        container: '#ffdad6',   // Цвет контейнера с ошибкой
        onContainer: '#410002'  // Цвет текста/иконок на контейнере ошибки
      },
      
      // Цвета фона - основной фон приложения
      background: {
        main: '#fffbfe',        // Основной цвет фона (белый с легким розовым оттенком)
        onBackground: '#1c1b1f' // Цвет текста на основном фоне
      },
      
      // Цвета поверхностей - для карточек, панелей, модальных окон
      surface: {
        main: '#fffbfe',        // Основной цвет поверхности
        onSurface: '#1c1b1f',   // Цвет текста на основной поверхности
        variant: '#e7e0ec',     // Вариант поверхности (слегка отличается от основной)
        onVariant: '#49454f',   // Цвет текста на варианте поверхности
        
        // Контейнеры с различными уровнями высоты (для создания глубины)
        container: {
          lowest: '#ffffff',    // Самый светлый контейнер (наивысший уровень)
          low: '#f7f2fa',       // Светлый контейнер
          main: '#f3edf7',      // Основной контейнер
          high: '#ece6f0',      // Темный контейнер
          highest: '#e6e0e9'    // Самый темный контейнер (наименьший уровень)
        }
      },
      
      // Цвета контуров - для границ, разделителей
      outline: {
        main: '#79747e',        // Основной цвет контура
        variant: '#cac4d0'      // Вариант контура (более светлый)
      },
      
      // Служебные цвета
      shadow: '#000000',        // Цвет теней
      scrim: '#000000'          // Цвет затемнения (для модальных окон, оверлеев)
    },

    // Темная тема - цветовая схема для ночного режима
    dark: {
      // Основные цвета для темной темы
      primary: {
        main: '#d0bcff',        // Основной цвет (светло-фиолетовый)
        onPrimary: '#381e72',   // Цвет текста/иконок на основном цвете
        container: '#4f378b',   // Цвет контейнера с основным цветом
        onContainer: '#eaddff'  // Цвет текста/иконок на контейнере
      },
      
      // Вторичные цвета для темной темы
      secondary: {
        main: '#ccc2dc',        // Основной вторичный цвет (светло-серый)
        onSecondary: '#332d41', // Цвет текста/иконок на вторичном цвете
        container: '#4a4458',   // Цвет контейнера с вторичным цветом
        onContainer: '#e8def8'  // Цвет текста/иконок на контейнере
      },
      
      // Третичные цвета для темной темы
      tertiary: {
        main: '#efb8c8',        // Основной третичный цвет (светло-розовый)
        onTertiary: '#492532',  // Цвет текста/иконок на третичном цвете
        container: '#633b48',   // Цвет контейнера с третичным цветом
        onContainer: '#ffd8e4'  // Цвет текста/иконок на контейнере
      },
      
      // Цвета ошибок для темной темы
      error: {
        main: '#ffb4ab',        // Основной цвет ошибки (светло-красный)
        onError: '#690005',     // Цвет текста/иконок на цвете ошибки
        container: '#93000a',   // Цвет контейнера с ошибкой
        onContainer: '#ffdad6'  // Цвет текста/иконок на контейнере ошибки
      },
      
      // Цвета фона для темной темы
      background: {
        main: '#1c1b1f',        // Основной цвет фона (темно-серый)
        onBackground: '#e6e0e9' // Цвет текста на основном фоне
      },
      
      // Цвета поверхностей для темной темы
      surface: {
        main: '#1c1b1f',        // Основной цвет поверхности
        onSurface: '#e6e0e9',   // Цвет текста на основной поверхности
        variant: '#49454f',     // Вариант поверхности
        onVariant: '#cac4d0',   // Цвет текста на варианте поверхности
        
        // Контейнеры с различными уровнями высоты для темной темы
        container: {
          lowest: '#0f0d13',    // Самый темный контейнер (наивысший уровень)
          low: '#1c1b1f',       // Темный контейнер
          main: '#211f26',      // Основной контейнер
          high: '#2b2930',      // Светлый контейнер
          highest: '#36343b'    // Самый светлый контейнер (наименьший уровень)
        }
      },
      
      // Цвета контуров для темной темы
      outline: {
        main: '#938f99',        // Основной цвет контура
        variant: '#49454f'      // Вариант контура (более темный)
      },
      
      // Служебные цвета (одинаковые для обеих тем)
      shadow: '#000000',        // Цвет теней
      scrim: '#000000'          // Цвет затемнения
    }
  }
};
